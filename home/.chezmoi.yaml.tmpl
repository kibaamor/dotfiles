{{- $workpc := contains "-WKS-" .chezmoi.hostname -}}

# https://github.com/twpayne/chezmoi/issues/3257
pager: ""

{{/* Here we "export" the variables, so we can access them outside this file */ -}}
data:
  is_workpc: {{ $workpc }}

  uname_arch: "{{ output "uname" "-m" | trim }}"